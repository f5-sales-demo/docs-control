---
# Governed read-only policy caller. It has no model or publication credentials.
name: English-only Documentation Policy

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  audit:
    uses: f5-sales-demo/docs-control/.github/workflows/translation-audit.yml@2062cd557231a7827ad8e732a9c714987295efff
